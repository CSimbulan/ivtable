-add more sorting/toggle options (sort by ATK value)
-clean up code (repeated code)
-move renderSuggestions call out of app.render
-add other language options
-combine all toggle functions into one
-change filtering so a new stats array doesn't have to be generated every time
-fix bug in firefox where rows aren't fully colored (e.g. select lapras -> type 1131 in show only cp then toggle on and off lvl 15)\
-convert table to image and add save button